/**
 * Auth feature — local TypeScript types.
 * Full type definitions added in Phase 4.
 */

export interface LoginFormData {
  email: string;
  password: string;
}

export interface RegisterFormData {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
}
